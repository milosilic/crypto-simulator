# crypto-simulator

Static STACKBACK simulators (DCA, lump sum, dynamic DCA) with bundled BTC price and precomputed daily risk scores.

Risk scores in `data/btc-risk-daily.json` are generated offline; the computation tooling is maintained in a separate project.
